import { FunctionComponent } from "react";

const PainelHome:FunctionComponent = () =>{
    return(
        <div>
            Olá Recrutador(a), seja bem-vindo(a) ao seu painel de admissões!
        </div>
    );
}

export default PainelHome;