import { FunctionComponent } from "react";

const SideBar: FunctionComponent = () => {
  return (
    <nav className="bg-blue-600 flex flex-col h-screen">
      <ul>
        <li>Controle de ponto</li>
      </ul>
    </nav>
  );
};

export default SideBar;
